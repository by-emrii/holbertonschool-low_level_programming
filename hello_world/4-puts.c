#include <stdio.h>
/**
 * main - is the entry point of func with no arg entry
 * Description: puts func to print a string
 * Return: 0 meaning success
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
